<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow-sm">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/">BlogTube</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto align-items-center">
        <li class="nav-item">
          <a class="nav-link" href="/blog">Blog App</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/tube">Tube App</a>
        </li>
        <% if (locals.user) { %>
          <li class="nav-item">
            <a class="nav-link" href="/blog/add-new">+ Blog</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/tube/add-new">+ Video</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <img src="<%= user.profileImageURL %>" alt="Profile" class="rounded-circle me-2" width="35" height="35">
              <span class="fw-semibold"><%= user.fullName %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li><a class="dropdown-item" href="/user/logout">Logout</a></li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="btn btn-outline-light me-2" href="/user/signup">Sign Up</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-primary" href="/user/signin">Sign In</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<% if (locals.error) { %>
  <div class="alert alert-danger text-center mt-2" role="alert"><%= locals.error %></div>
<% } %>
